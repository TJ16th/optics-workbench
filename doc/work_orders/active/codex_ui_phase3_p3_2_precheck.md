# P3-2着手前の確認事項（P3-0/P3-1完了を受けて）

P3-0・P3-1は確認済み。P3-2（ズーム・フォーカス位置のスライダー操作）に進める前に、以下2点を先に確認・対応してから着手すること。

## 確認1：P003拡張がPhase 2の既存テストに影響していないか

P3-0で `apps/workbench-ui/src/domain/presets.ts` のP003に `FOCUS_G` / `OIS_G` / `zoom_positions` を追加した。P003はPhase 2（P2-2）のチャート受け入れ確認（アクロマート系の色収差カーブ表示等）で使われていたプリセットである。

- Phase 2で追加されたP003関連のE2E・テスト（`chart-theme:test`、P2-2のPlaywright E2E等）を再実行し、群定義の追加によって既存の解析結果・チャート表示・受け入れ条件が変化していないことを確認する。
- 影響がある場合（例：デフォルト構成でのspot/MTF等の数値が変わった等）、その内容と是非を報告する。

## 確認2：P004プリセット欠落の記録

P3-0の報告で、UI仕様v0.3 9章に定義されているP004（簡易ダブルガウス）が `apps/workbench-ui/src/domain/presets.ts` に未実装であることが判明した。Phase 1〜2のどこかで漏れていたと見られる。

- `doc/reports/issues_backlog.md` に以下の内容でIssue草案を追記する（AGENTS.mdの「GitHub Issue運用」節のフォーマットに従う）：
  - タイトル：`UI実装にP004プリセット（簡易ダブルガウス）を追加する`
  - ラベル案：`ui`
  - 背景：UI仕様9章にP004の定義があるが、実装側presets.tsに存在しないことがP3-0で判明した。
  - 対応案：仕様9章のP004定義に従い、presets.tsへ追加する。近軸検証を行い、9.1節の数値検証規約に従って凍結する。
  - 受け入れ条件：P004がプリセット一覧に表示され選択できる。近軸トレースが破綻しない。
- 今回のタスクではP004の実装には着手しない（Issue登録のみ）。

## 完了後

上記2点の確認結果を報告した上で、P3-2に進んでよい。
