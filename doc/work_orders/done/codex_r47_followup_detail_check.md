# R47完了報告の詳細確認

R47（`codex_r47_annulus_inner_radius_ui_visibility.md`）の完了報告のうち、以下の点がまだ十分に確認できていない。改めて詳細を報告してください。

## 確認したいこと

1. **P005のSTOP面のaperture定義について**：
   - 調査開始時点（修正前）の実際の値を、JSON/YAML相当の生データとして示してください（`shape`フィールドの値、`semi_diameter_mm`または`outer_semi_diameter_mm`/`inner_semi_diameter_mm`の値）。
   - 「`shape: circle`だった」という理解で合っていますか。もし違っていれば、正確な状態を教えてください。

2. **修正内容**：
   - 実際にどう修正したか（`shape: annulus`に変更し、`inner_semi_diameter_mm`・`outer_semi_diameter_mm`を設定したか）を、修正後の生データとして示してください。
   - inner半径・outer半径には、それぞれ具体的にどの値を設定しましたか。その値はどこから決めましたか（R42/R44/R45で議論していた「副鏡M2のsemi_diameter_mmと一致させるべき」という方針を踏まえましたか）。

3. **光線追跡への影響**：
   - この修正前は、annulus形状ではなかった（＝内径側の遮蔽が実際には効いていなかった）ということになりますが、修正前の光線追跡で、実際にinner側を通る光線が誤って通過扱いになっていなかったか確認しましたか。していなければ、確認してください（Golden Testや既存のannulus関連テストが、実は機能しない条件でパスしていた可能性があるため）。

4. **Systemタブでの表示・編集**：
   - 元のタスク（R47）の目的だったSystemタブでの「inner半径・outer半径の表示（可能なら編集）」は、実際にUIへ実装されましたか。
   - 実装されていれば、Systemタブの該当箇所のスクリーンショットを`doc/reports/screenshots/`配下に保存し、報告に含めてください（R46の保存規約に従う）。
   - 実装されていなければ、その理由（範囲外と判断した、後続タスクに委ねた等）を明記してください。

5. **既存テストへの影響**：
   - この修正（annulus形式への変更）で、既存のGolden Test・annulus関連テストの結果が変わっていないか確認しましたか。特に、R42で確認した近軸検証値（M1後-650mm、M2後+1050mm等）に影響が無いことを確認してください。

## 完了条件

- 上記5点それぞれについて、具体的な数値・コード・スクリーンショットを伴う回答が提示されている。
- Systemタブでの表示が未実装であれば、その状態を正直に報告する（実装されたと誤解を与える表現をしない）。
