# R24b push前の最終確認：正本ファイルの差分明示

## 背景

R24bの確認フェーズ（未pushコミット36件、123ファイル変更）は完了した。pushの承認判断のため、以下を明示的に確認してから、人間の最終承認を待つ。

## 作業

1. `git diff origin/master..HEAD --stat -- AGENTS.md doc/engine_spec.md doc/ui_spec.md`を実行し、この3ファイルの変更行数を個別に示す。
2. それぞれの変更内容を簡潔に要約する（AGENTS.mdなら「同一コミットルール追記、報告言語ルール、build_info確認ルール等」のように、今回のセッションでどのルールが追加されたかを箇条書きで）。
3. `doc/engine_spec.md`・`doc/ui_spec.md`に変更が無い場合、その旨を明記する（無変更もそれ自体が正しい報告）。

## 完了条件

- 3ファイルそれぞれの差分有無・変更概要が報告されている。

## この後

この報告を人間が確認し、問題なければ`git push origin master`の実行許可を出す。
