# R130：公開push準備（202コミット） 指示書（Codex向け・キュー追加）

## 背景

`2026-07-15_unmerged_implementation_check.md`（R129として台帳管理）で、ローカル`master`の202コミットが未pushかつ`origin/master`と分岐（remote側に`be97d58`＝Issue化workflowのbacklog注記1件）と判明。人間がpush準備の実施を承認した。本指示書は連続作業キューの**末尾への追加**（R128の後）。キュー運用ルール適用。

**最重要**：本タスクで`git push`は実行しない。push可能な状態まで整えて結果を報告し、人間の明示承認を待つ。

## 方針（人間決定済み）

- **remote取込はmerge方式。rebaseは禁止**。理由：202コミットのハッシュが全て変わると、台帳・完了報告書群が参照するコミットハッシュ記録（`1f4ac20`・`9ba435b`等多数）が無効化されるため。

## 作業

### 1. active指示書の整理

R129で特定した「同名doneがHEADに存在する未追跡active 40件」を削除する（削除対象の一覧を報告書へ記載。R128・R130等、現役の指示書は削除しない）。あわせて、R55同期の「削除非対応」仕様による再出現の再発防止案（同期スクリプトのdone移動済み除外等）を提案として報告書へ記載する（同期スクリプトの変更自体は行わない）。

### 2. `be97d58`のmerge取込

1. `git merge`（rebase禁止）で`origin/master`の`be97d58`を取り込む。
2. **issues_backlog.mdのconflict解消規則**：R125整理後の構造（34件）を正とし、`be97d58`が付与したIssue番号注記のうち**生き残っている項目への注記は反映**する。R125で削除・統合済みの項目に対する注記は取り込まず、該当するGitHub Issue番号を「実装完了済みのためclose推奨」一覧として報告書へ記載する（Issueのclose自体は人間の判断）。
3. merge commitのメッセージに解消規則を明記する。

### 3. 検証

- エンジン全テスト（pytest）＋`npm run ci`＋`ui:build:pseudo`をmerge後のHEADで全実行。
- API/UI再起動→`/v1/meta` build_info一致確認。

### 4. 公開前チェック

1. **PIIスキャン**：`python scripts/pii_scan.py`を実行。検出は「既知（G1時点から記録済みの絶対パス等）」と「未push202コミットで新規に入ったもの」に分類し、新規分は内容を精査（個人情報・秘匿情報の有無を判定）して報告。**疑わしいものが1件でもあれば、それを含むファイルの一覧と修正案を提示して停止**（自分で履歴書き換えはしない）。
2. **G5 push checklist**：`doc`内の既存push checklist（G5成果物）を再実行し、各項目の判定を報告書へ記載。
3. 未追跡・未コミットファイルが残っていないこと（報告書・スクリーンショットは通常コミットへ含める）。

### 5. push承認用サマリー

報告書に以下を含める：merge後のHEADハッシュ、push対象コミット数、feat/fix等の種別集計（R129報告の更新版）、CI結果、PIIスキャン判定、checklist判定、close推奨Issue一覧、「push実行コマンドと想定結果」。**ここで停止し、pushは行わない**。

## 完了条件

- 作業1〜5完了（push未実施）。merge conflictの解消内容がdiffで確認可能。
- 全テストgreen、PIIスキャン判定済み、checklist全項目判定済み。
- 報告書：`doc/reports/2026-07-15_r130_push_preparation.md`（日付は完了日）、冒頭に着手前見積もり。

## 注意

- amend禁止・タスク別コミット等のR104ルール適用。
- PIIスキャンで新規の疑義が出た場合はバッチ実行モードの例外とし、後続作業（4-2以降）を止めて報告する（公開前チェックが目的のため）。
