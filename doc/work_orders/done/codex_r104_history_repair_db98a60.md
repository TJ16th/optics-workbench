# R104：db98a60混在コミットの履歴修復 指示書（Codex向け・条件付き承認）

## 背景

並行進行していたR102のコミットとR103のamendが競合し、現行HEAD`db98a60`にR102完了資料（報告書・ベンチJSON）とR103凡例修正が混在した。ファイル内容・テスト結果は保全されている。Codexから「作業ツリーを保持したまま`dd6dcbe`までコミット境界を戻し、R103実装とR102完了資料を別コミットとして積み直す」提案があり、人間が**条件付きで承認**した。以下の条件をすべて満たすこと。

## 承認条件（必須・順守）

1. **事前確認：リモートへのpush状態**。`git log origin/main..HEAD`等で、書き換え対象コミット（`dd6dcbe`より後の全コミット）が未pushであることを最初に確認する。**1つでもpush済みなら作業を中止し、その旨を報告して指示を仰ぐこと**（公開履歴の書き換えは別問題になるため）。
2. **`dd6dcbe`（R102エンジン実装コミット）には一切手を触れない**。戻すのはコミット境界のみ（`git reset --soft`等、作業ツリー保持)で、`dd6dcbe`自体とそれ以前の履歴は不変であること。
3. **積み直しのコミット分割**は最低限以下とする：
   - R103実装（UI変更一式）
   - R102完了資料（報告書・ベンチJSON）
   - R103凡例修正
   - （その他db98a60までに混在していた変更があれば、由来タスクごとに分離）
   各コミットメッセージに由来タスク番号（R102/R103）を明記する（R8cの同一コミットルール・R46以降の慣行に準拠）。
4. **内容一致検証**：積み直し完了後、旧`db98a60`時点のツリーと新HEADのツリーが完全一致することを`git diff db98a60 HEAD`が空であることで検証し、報告書へ記載する（旧コミットはreflogまたは事前に付けた退避ブランチ/タグで参照可能にしておくこと）。
5. **ハッシュ対応表**：旧→新の対応（`9c0e02c`→新R103実装コミット、`db98a60`→分割後の各コミット等）を報告書に表で記載する。R102報告書（`2026-07-15_r102_evaluate_trace_sharing.md`）の実環境反映節が参照する`9c0e02c`が旧ハッシュになる旨の注記を、R102報告書側へ追記してよい（本文の書き換えはせず、末尾追記とする）。
6. **再検証**：積み直し後の新HEADで`npm run ci`とエンジンテスト（R102追加テスト含む）を再実行しグリーンを確認。API/UI再起動の上、`/v1/meta`の`build_info.git_commit`が新HEADと一致することを確認する。
7. **再発防止**：AGENTS.mdへ「並行タスク進行中はamendを禁止し、常に新規コミットとする（履歴修復が必要になった場合は本R104を前例として人間の明示承認を得る）」を追記する。

## 完了条件

- 承認条件1〜7がすべて履行され、証跡（コマンド出力・diff空の確認・対応表）が報告書に記載されている。
- R103の完了報告書（`2026-07-15_r103_ui_phase3_same_screen_workspace.md`、未着分）は、本修復後の新ハッシュ基準で作成・提出する。スクリーンショット3枚は提出済みのため再取得不要（内容に変更がなければ）。

## 注意

- 本タスク中は新規の機能変更を行わないこと（純粋な履歴整理のみ)。
- 報告書は`doc/reports/2026-07-15_r104_history_repair_db98a60.md`（日付は完了日）へ。
