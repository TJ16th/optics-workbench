# UI Phase 3 P3-1 Report

## Scope

- Task: P3-1 group definition display and editing UI.
- Date: 2026-07-09

## Implemented

- Added an `OpticalGroup` type and changed `OpticalSystem.groups` to use it.
- Added a Group editor to the System tab next to the Surface Table.
- Supported group display, creation, deletion, and editing of:
  - group ID
  - name
  - from_surface
  - to_surface
- Added local group validation/warnings:
  - duplicate group IDs
  - unknown surface references
  - invalid surface order
  - overlapping group surface ranges
- Connected group edits to dirty state:
  - `system_dirty` becomes active
  - registered `system_id` / hash are cleared
  - stale trace/chart/focus outputs are cleared
  - analysis is marked dirty
- Added ja/en i18n strings for the new UI.
- Added an E2E test covering P003 group display, overlap warning, add/remove, invalid range warning, and `system_dirty`.

## Verification

- `npm.cmd run ui:build`: passed
  - Existing Vite warnings only: dependency `"use client"` directives and large chunk warning.
- `npm.cmd run i18n:coverage`: passed
- `npx.cmd playwright test --config apps/workbench-ui/playwright.config.ts apps/workbench-ui/tests/e2e/analysis-conditions.spec.ts -g "group edits"`: passed, 1 test.

## Notes

- P003 intentionally has overlapping group ranges (`FOCUS_G` and `OIS_G`) so the P3-1 warning path is visible immediately.
- This task does not yet implement slider-driven zoom/focus motion; that is P3-2.
